import React, {useState} from 'react';
import {StyleSheet, View, Text, Pressable} from 'react-native';

export default function HomeList(prop) {
  return (
     //refreash flat list
    <View style={styles.container}>
     
    </View>
  );
}
const styles = StyleSheet.create({
 
});
